import React from 'react';

function Rules() {
  /*
   このようにstateを条件で作成してはいけない
   if (条件) {
     const [state, setState] = useState('適当なstate');
   }
   */
  return <div>ルールをまとめました</div>;
}

export default Rules;
